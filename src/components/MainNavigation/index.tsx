import React from 'react';
import { MyComponent } from './styled';

type Props = {};

const MainNavigation = (props: Props) => {
    return <MyComponent>Styled</MyComponent>;
};

export default MainNavigation;
